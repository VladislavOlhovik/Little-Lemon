import { LemonSvg } from '@/icons';

import * as SC from './preloader.styled';

export const Preloader = () => {
  return (
    <SC.PreloaderContainer>
      <LemonSvg />
    </SC.PreloaderContainer>
  );
};
